# Robolectric 4.16 ships no image for the repository's compileSdk (37), and defaults to it, so it
# refuses to pick an SDK at all. Pin the newest level it does support; the two cases that are
# API-level dependent — the device-credential authenticator combination — override this per test.
sdk=35
