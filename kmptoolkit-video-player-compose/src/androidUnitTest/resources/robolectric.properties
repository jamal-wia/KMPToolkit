# Robolectric 4.16 ships no image for the repository's compileSdk (37), and defaults to it, so it
# refuses to pick an SDK at all. Pin the newest level it does support; nothing this module tests is
# API-level dependent.
sdk=35
